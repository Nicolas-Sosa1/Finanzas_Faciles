<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="FinanzasFaciles.Controls.FlyoutMenuItemView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    HorizontalOptions="Fill"
    InputTransparent="False">
    
    <Border
        x:Name="ItemRoot"
        StrokeThickness="0"
        Padding="0"
        MinimumHeightRequest="52"
        HorizontalOptions="Fill"
        VerticalOptions="Fill"
        BackgroundColor="Transparent">
        <Label
            x:Name="ItemTitle"
            Style="{StaticResource ShellFlyoutMenuItemLabelStyle}"
            Padding="28,0,28,0"
            HorizontalOptions="Fill"
            VerticalOptions="Center"
            VerticalTextAlignment="Center"
            HorizontalTextAlignment="Start"
            LineBreakMode="NoWrap" />
    </Border>
    <VisualStateManager.VisualStateGroups>
        <VisualStateGroupList>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal">
                    <VisualState.Setters>
                        <Setter Property="BackgroundColor" TargetName="ItemRoot" Value="Transparent" />
                        <Setter Property="Label.TextColor" TargetName="ItemTitle"
                                Value="{AppThemeBinding Light={StaticResource FlyoutMenuItemTextLight}, Dark={StaticResource FlyoutMenuItemTextDark}}" />
                        <Setter Property="Label.FontAttributes" TargetName="ItemTitle" Value="None" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="PointerOver">
                    <VisualState.Setters>
                        <Setter Property="BackgroundColor" TargetName="ItemRoot" Value="{StaticResource BrandGreenSoft}" />
                        <Setter Property="Label.TextColor" TargetName="ItemTitle" Value="White" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Selected">
                    <VisualState.Setters>
                        <Setter Property="BackgroundColor" TargetName="ItemRoot" Value="{StaticResource BrandGreen}" />
                        <Setter Property="Label.TextColor" TargetName="ItemTitle" Value="White" />
                        <Setter Property="Label.FontAttributes" TargetName="ItemTitle" Value="Bold" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateGroupList>
    </VisualStateManager.VisualStateGroups>
</ContentView>
